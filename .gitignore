# Dependencies
node_modules/
frontend/node_modules/
api/vendor/

# Build output (generated by `vite build`; source lives in frontend/)
# Anchored to repo root so it doesn't also match frontend/public/, which is the
# real source directory for hand-authored static files (e.g. .htaccess).
frontend/dist/
/public/
deploy/

# Secrets & runtime data
.env
.env.vakaros
storage/

# Local sample data (private, large binaries)
01_Sample Data/

# OS / editor
.DS_Store
*.log
.vscode/
.idea/

# TS incremental build info
*.tsbuildinfo
